package com.example.data.model

enum class UserRole(val label: String, val description: String) {
    CHILD("Child", "I want to share my day"),
    PARENT("Parent", "I want to stay connected with my child")
}
